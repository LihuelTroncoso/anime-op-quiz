{
	"name": "anime-op-quiz",
	"private": true,
	"workspaces": [
		"apps/*",
		"packages/*"
	],
	"scripts": {
		"migrateSql": "bunx --bun prisma migrate dev --name",
		"updateSql": "bunx --bun prisma generate",
		"dev": "concurrently -n api,web -c yellow,blue \"bun run --cwd apps/api dev\" \"bun run --cwd apps/web dev\"",
		"build": "bun run --cwd apps/api build && bun run --cwd apps/web build",
		"start": "concurrently -n api,web -c yellow,blue \"bun run --cwd apps/api start\" \"bun run --cwd apps/web start\""
	},
	"dependencies": {
		"@prisma/adapter-libsql": "^7.6.0",
		"@prisma/adapter-pg": "^7.6.0",
		"@prisma/client": "^7.6.0",
		"@prisma/client-runtime-utils": "7.6.0",
		"dotenv": "^17.3.1",
		"hono": "^4.10.6",
		"pg": "^8.20.0"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.10",
		"@types/pg": "^8.20.0",
		"concurrently": "^9.2.1",
		"prisma": "^7.6.0",
		"tsx": "^4.21.0"
	}
}
